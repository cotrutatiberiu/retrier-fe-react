import { useEffect } from 'react';
import './App.css'
import { useAppSelecotr } from './hooks/reduxHooks'

function App() {
  // const firstName = useAppSelecotr(state => state.profile.firstName);

  return (
    <div></div>
  )
}

export default App
